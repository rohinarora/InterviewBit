class Solution:
    # @param A : integer
    # @return a strings
    def countAndSay(self, num):
        result=""
        num=str(num)
        while (i<len(num)):
            
        for i in range(len(num)):
            if


obj1=Solution()
print (obj1.countAndSay(21))
